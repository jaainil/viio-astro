---
import Button from "../components/Button.astro";
import type { Link } from "../types";

interface Props {
  title: string;
  text: string;
  cta?: Link;
}

const { title, text, cta } = Astro.props;
---

<div class="sy-introducing">
  <div class="u-container container">
    <div class="inner">
      <h2 class="u-heading title" data-sy-reveal="words">{title}</h2>
      <p class="u-text" data-sy-reveal="lines">
        {text}
      </p>
      {cta ? <Button {...cta} arrow class="cta" /> : null}
      <div data-gl-place></div>
    </div>
  </div>
</div>

<style>
  .container {
    padding: 6rem 0;
    position: relative;
  }

  .inner {
    position: relative;
    z-index: 9;
    max-width: 50rem;
    margin: 0 auto;
    padding: 4rem 3rem;
    text-align: center;
    gap: 1.5rem;
    display: flex;
    justify-content: center;
    align-items: center;
    flex-direction: column;
    border-radius: 2rem;
    background: var(--color-white);
    box-shadow: 0 20px 60px rgba(var(--rgb-brand-1), 0.1);
    border: 1px solid var(--color-brand-3);
  }

  h2 {
    --fs: 3rem;
    --lh: 1.1;
    max-width: 20em;
    color: var(--color-text);
  }

  p {
    max-width: 40em;
    font-size: var(--font-size-lg);
    line-height: 1.7;
    color: var(--color-text-alt);
  }

  .cta {
    margin-top: 1rem;
  }

  [data-gl-place] {
    --d: 0.1;
    --s: 18rem;
    position: absolute;
    right: 0%;
    top: 0;
    margin-top: -2rem;

    @media (--mq-tablet-sm) {
      transform: translateX(-10%);
      left: 0;
    }
  }
</style>
